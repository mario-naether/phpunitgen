{% autoescape false %}
<?php
{%  if class.namespace %}
namespace {{ class.namespace }};
{% endif %}
/**
*
* {{ class.name }}
*
* @package {{ class.namespace }};
*/
class {{ class.name }} extends PHPUnit_Framework_TestCase
{

    /**
     * @var \{{ originalClass.name }}
     */
    protected $fixture;

    public function setUp()
    {
        $this->fixture = new \{{ originalClass.name }}();
    }

    public function tearDown()
    {
        $this->fixture = null;
    }

{% for method in class.methods %}
    {{ method }}
{% endfor %}
}
{% endautoescape %}